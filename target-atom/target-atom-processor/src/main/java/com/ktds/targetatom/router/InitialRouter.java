package com.ktds.targetatom.router;

/*import org.apache.camel.CamelContext;
import org.apache.camel.builder.RouteBuilder;

import com.ktds.targetatom.bean.AuditHandler;

public class InitialRouter extends RouteBuilder{

	private String from;
	private String to;
		
	public InitialRouter(CamelContext context, String from, String to) {
		super(context);
		this.from = from;
		this.to = to;
	}
	@Override
	public void configure() throws Exception {
		//from(from).bean(AuditHandler.class, "insertFirstCollAditHst").to(to);
		from(from).to(to);
	}
}*/
